cd  ws_tum_percept 

# clone peract
git clone https://github.com/peract/peract.git 
conda create --name peract python=3.8 

# activate conda environment
conda activate peract 
cd peract

# setup peract environment
virtualenv -p $(which python3.8) --system-site-packages peract_env  
source peract_env/bin/activate
pip install --upgrade pip


# setup PyRep
cd ~/ws_Coppelia
git clone -b 4.6.0.beta https://github.com/stepjam/PyRep
cd PyRep

# added this to ~/.bashrc
# export COPPELIASIM_ROOT=<EDIT ME>/PATH/TO/COPPELIASIM/INSTALL/DIR
# export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$COPPELIASIM_ROOT
# export QT_QPA_PLATFORM_PLUGIN_PATH=$COPPELIASIM_ROOT

source ~/.bashrc
conda actiavate peract

# commented out python_requires=">=3.10" in line 38 of setup.py


